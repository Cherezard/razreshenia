
#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Если Не (Группа Или ПользователиВызовСервераРазрешения1C.ЭтоПолноправныйПользователь()) Тогда
		Сообщить("Отправители редактируются администратором системы!");
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
